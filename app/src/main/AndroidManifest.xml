<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="tr.edu.yildiz.virtualcabinet">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.VirtualCloset">
        <activity android:name="tr.edu.yildiz.virtualcabinet.ActivityListActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <activity
            android:name="tr.edu.yildiz.virtualcabinet.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name="tr.edu.yildiz.virtualcabinet.AddActivityActivity" />
        <activity android:name="tr.edu.yildiz.virtualcabinet.CombineListActivity" />
        <activity android:name="tr.edu.yildiz.virtualcabinet.CombineActivity" />
        <activity android:name="tr.edu.yildiz.virtualcabinet.ClothesListActivity" />
        <activity android:name="tr.edu.yildiz.virtualcabinet.DrawerListActivity" />
        <activity android:name="tr.edu.yildiz.virtualcabinet.AddCombineActivity" />
        <activity android:name="tr.edu.yildiz.virtualcabinet.AddClothesActivity" />
        <activity android:name="tr.edu.yildiz.virtualcabinet.AddDrawerActivity" />
        <receiver android:name="tr.edu.yildiz.virtualcabinet.service.PowerConnectionReceiver"
            tools:ignore="Instantiatable" />
        <activity android:name="tr.edu.yildiz.virtualcabinet.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.ACTION_POWER_CONNECTED"/>
                <action android:name="android.intent.action.ACTION_POWER_DISCONNECTED"/>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

</manifest>